{
  "environments": {
    "test": {
      "env": {
        "AWS_SECRET_ACCESS_KEY": "er7By0oo5SgXF4gAvAlyJ/gkImg4ua1hghJAO+68",
        "AWS_ACCESS_KEY_ID": "AKIARFDUSM66YKPLKNQ2",
        "DATABASE_CLEANER_ALLOW_REMOTE_DATABASE_URL": true,
        "RAILS_ENV": "test",
        "RACK_ENV": "test",
        "S3_BUCKET": "books-ubi",
        "SENTRY_DSN": "fake"
      },
      "addons":[
         "heroku-redis",
         "heroku-postgresql"
      ],
      "scripts": {
        "test-setup": "gem install cucumber-rails && gem install rspec && gem install rubocop",
        "test": "rubocop . && rspec && yarn install && bundle exec rails webpacker:compile && cucumber"
      }
    },
    "review":{
        "scripts": {
            "postdeploy": "bundle exec rake db:schema:load db:seed"
        },
        "addons":[
            "heroku-redis",
            "heroku-postgresql"
        ]
    }
  }
}

