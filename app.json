{
  "environments": {
    "test": {
      "addons":[
         "heroku-redis",
         "heroku-postgresql"
      ]
    "scripts": {
        "test-setup": "gem install cucumber-rails",
        "test": "cucumber"
      }
    }
  }
}

